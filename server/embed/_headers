# Only the proxy embed is isolated. The UI page must NOT be: a COEP
# require-corp document can't embed the embed.svg iframe when the embed is
# hosted on another origin (it would need CORP: cross-origin).
/embed.svg
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
